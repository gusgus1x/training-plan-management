import { apiSuccess } from "../../../../lib/api/response";import { readJsonObject,readPositiveId } from "../../../../lib/api/validation";import { createProtectedRoute,type ProtectedRouteOptions } from "../../../../lib/auth/guard";import { employeeService,type EmployeeService } from "../../../../lib/employees/service";import { parseUpdateEmployee } from "../../../../lib/employees/validation";
type C={params:Promise<{employeeId:string}>};type D={auth?:ProtectedRouteOptions;service?:EmployeeService};const opts=(auth?:ProtectedRouteOptions)=>({...auth,allowedRoles:["HRD_CENTER","HRD_FACTORY"] as const});const id=async(c:C)=>readPositiveId((await c.params).employeeId,"employeeId");
export const createGetEmployeeHandler=(d:D={})=>createProtectedRoute<C>(async(_r,p,c)=>apiSuccess({employee:await(d.service??employeeService).get(p,await id(c))}),opts(d.auth));
export const createUpdateEmployeeHandler=(d:D={})=>createProtectedRoute<C>(async(r,p,c)=>apiSuccess({employee:await(d.service??employeeService).update(p,await id(c),parseUpdateEmployee(await readJsonObject(r)))}),opts(d.auth));
export const createDeleteEmployeeHandler=(d:D={})=>createProtectedRoute<C>(async(_r,p,c)=>apiSuccess({employee:await(d.service??employeeService).remove(p,await id(c))}),opts(d.auth));
export const GET=createGetEmployeeHandler();export const PATCH=createUpdateEmployeeHandler();export const DELETE=createDeleteEmployeeHandler();
